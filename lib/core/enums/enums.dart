// ignore_for_file: constant_identifier_names

enum SnackbarType { success, error, info }

enum AuthenticationContext {
  mobile,
  webSidebar,
}

enum FormStep {
  serviceSection,
  identification,
  personalInformation,
  insuranceAndBanking,
  locationForm,
  termsAndAgreement,
  review,
}

enum AddressSelectionType { Current, Manual }

enum OtpType { Phone, Email }

enum Role { Client, Admin, Pro }

enum NotificationType {
  registration,
  service,
}
